apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: cert-issuer
spec:
  acme:
    email: "hamzahsahal9@gmail.com"
    server: "https://acme-v02.api.letsencrypt.org/directory"
    privateKeySecretRef:
      name: issuer
    solvers:
      - dns01:
          route53:
            region: eu-west-2
            hostedZoneID: Z00169631W0W1DW2JNVMM  
        selector:
          dnsZones:
            - "eks.hamzahsahal.com" 
      - http01:
          ingress:
            class: nginx


    